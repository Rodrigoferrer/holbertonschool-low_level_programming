#include "main.h"
#include <stdio.h>

/**
 * mul - Function that multiplies two integers
 * @a: first parameter
 * @b: second parameter
 * Return: resultado
 */

int mul(int a, int b)
{
	int resultado;

	resultado = a * b;
	return (resultado);
}
