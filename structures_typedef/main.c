#include "struct.h"
int main()
{
        scanf(
	printf("The name of the animal is: %s\nThe age is: %f\n The owner is: %s\n", estructura1.name, estructura1.age, estructura1.owner);
        printf("The name of the animal is: %s\nThe age is: %f\n The owner is: %s\n", formulario1.name, formulario1.age, formulario1.owner);
        return (0);
}
