#include <stdio.h>
#include "main.h"

/**
 * funcion que devuelve el largo de un string
 */
int _strlen(char *s)
{
	int contador = 0;

	for (; s[contador] != '\0'; contador++)
	{
	}

	return (contador);
}
