
#include <stdio.h>
#include "lists.h"

/**
 * _strlen - Function that returns the length of a string
 * @s: String
 * Return: length
 */

int _strlen(char *s)
{
	int len;

	for (len = 0; s[len] != '\0'; len++)
	{
	}
	return (len);
}
